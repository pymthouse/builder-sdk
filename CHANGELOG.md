# Changelog

## 0.1.0

- Initial release: `@pymthouse/builder-sdk` with OIDC/OAuth via `oauth4webapi` (OpenID-certified RP).
- Builder REST, Usage API, device approval (RFC 8693), signer session exchange, optional RFC 8628 device polling, RFC 9068 JWT access token validation.
